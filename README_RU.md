<p align="center">
  <img src="docs/images/pyrl_banner.png" alt="Pyrl Banner" width="100%">
</p>

<p align="center">
  <img src="docs/images/pyrl_logo.png" alt="Pyrl Logo" width="150">
</p>

<h1 align="center">Pyrl — Гибридный язык программирования Python + Perl</h1>

<p align="center">
  <strong>Мощь Perl. Простота Python. Один язык.</strong>
</p>

<p align="center">
  <a href="#-особенности">Особенности</a> •
  <a href="#-быстрый-старт">Быстрый старт</a> •
  <a href="#-документация">Документация</a> •
  <a href="#-примеры">Примеры</a> •
  <a href="#-инструменты">Инструменты</a>
</p>

<p align="center">
  <a href="README.md">English</a> | <strong>Русский</strong>
</p>

---

## 🎯 О проекте

**Pyrl** (Python + Perl) — это современный гибридный язык программирования, объединяющий выразительную мощь Perl с читаемостью и простотой Python. Язык использует сигильную систему переменных Perl (`$scalar`, `@array`, `%hash`, `&function`) в сочетании с Python-синтаксисом управления потоком и отступами.

### Ключевые возможности

- 🔥 **Сигильные переменные** — интуитивное понимание типа по префиксу
- 🐍 **Python-синтаксис** — знакомые конструкции `if`, `for`, `while`, `def`
- 🐪 **Perl-мощь** — регулярные выражения, встроенные операторы
- 🗄️ **SQLite интеграция** — нативная работа с базами данных
- 🌐 **Web-сервер** — встроенный HTTP-сервер с REST API
- 🤖 **AI-модель** — предобученная модель для генерации кода

---

## ✨ Особенности

### Сигильная система переменных

```pyrl
$name = "Pyrl"          # Скаляр ($)
@numbers = [1, 2, 3]    # Массив (@)
%config = {             # Хеш (%)
    "host": "localhost",
    "port": 8080
}
&handler = {            # Функция (&)
    return "Hello!"
}
```

### Python-подобный синтаксис

```pyrl
def greet($name):
    if $name:
        return "Hello, " + $name + "!"
    else:
        return "Hello, World!"

for $i in range(5):
    print($i)
```

### Perl-стиль регулярных выражений

```pyrl
$text = "Hello, World!"
if $text =~ m/World/:
    print("Found!")

$result = $text =~ s/World/Pyrl/
```

### Web-приложения

```pyrl
$app = {
    handle: &handle_request
}

def handle_request($req):
    %response = {
        "status": 200,
        "body": "Hello from Pyrl!"
    }
    return %response
```

---

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/pyrl-lang/pyrl.git
cd pyrl

# Установка зависимостей
pip install -r requirements.txt
```

### Запуск

```bash
# Интерактивная консоль
python pyrl_cli.py

# Выполнение файла
python pyrl_cli.py examples/01_hello_world.pyrl

# Web-сервер
python scripts/run_web_app.py examples/web_server_auth.pyrl
```

### Docker

```bash
# Запуск сервера
docker-compose up -d server

# Консоль
docker-compose run console
```

---

## 📚 Документация

| Документ | Описание |
|----------|----------|
| [**Документация языка (RU)**](documents/DOCUMENTATION_RU.md) | Полное описание синтаксиса и возможностей |
| [**Language Documentation (EN)**](documents/DOCUMENTATION_EN.md) | Complete syntax and features reference |
| [**VSCode Extension**](documents/VSCODE_PLUGIN.md) | Расширение для VSCode |
| [**AI Model**](documents/AI_MODEL.md) | Обучение и использование модели |
| [**Docker Services**](docker/README.md) | Контейнеризация и деплой |
| [**Roadmap**](PROJECT_ROADMAP.md) | План развития проекта |
| [**CHANGELOG**](CHANGELOG.md) | История изменений |

---

## 🛠️ Инструменты

### VSCode Extension

Плагин для Visual Studio Code с подсветкой синтаксиса, автодополнением и сниппетами.

```bash
cd vscode-pyrl
code --install-extension .
```

**Возможности:**
- 🎨 Подсветка синтаксиса
- 📝 Сниппеты для быстрых конструкций
- 🔍 Автодополнение переменных

### Генератор AI-модели

Обучите собственную модель для генерации Pyrl-кода:

```bash
# Генерация модели
python scripts/generate_model.py

# Обучение
python scripts/train_model.py --epochs 10

# Инференс
python scripts/model_inference.py
```

### Предобученная модель

В репозитории включена предобученная модель:
- **Расположение:** `models/pyrl-model/`
- **Токенизатор:** BPE, 10,000 токенов
- **Архитектура:** Transformer-based

---

## 📁 Структура проекта

```
pyrl/
├── src/
│   └── core/
│       ├── lark_parser.py      # Парсер LALR
│       └── vm/
│           ├── vm.py           # Виртуальная машина
│           ├── builtins.py     # Встроенные функции
│           ├── builtins_db.py  # SQLite функции
│           └── builtins_http.py # HTTP функции
├── vscode-pyrl/                # VSCode расширение
├── scripts/
│   ├── generate_model.py       # Генератор модели
│   └── train_model.py          # Обучение модели
├── models/pyrl-model/          # Предобученная модель
├── examples/                   # Примеры кода
├── docker/                     # Docker конфигурации
└── documents/                  # Документация
```

---

## 📋 Примеры

| Файл | Описание |
|------|----------|
| [01_hello_world.pyrl](examples/01_hello_world.pyrl) | Hello World |
| [01_variables.pyrl](examples/01_variables.pyrl) | Переменные |
| [04_functions.pyrl](examples/04_functions.pyrl) | Функции |
| [06_classes.pyrl](examples/06_classes.pyrl) | Классы |
| [08_builtins.pyrl](examples/08_builtins.pyrl) | Встроенные функции |
| [20_perl_regex.pyrl](examples/20_perl_regex.pyrl) | Регулярные выражения |
| [web_server_auth.pyrl](examples/web_server_auth.pyrl) | Web-сервер с авторизацией |

---

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# Конкретный тест
python -m pytest tests/test_vm.py -v
```

---

## 🗺️ Дорожная карта

См. [PROJECT_ROADMAP.md](PROJECT_ROADMAP.md) для плана развития проекта.

### Текущая версия: 2.3.0

- ✅ SQLite интеграция
- ✅ Web-сервер с REST API
- ✅ AI-модель для генерации кода
- ✅ VSCode расширение

### Планы

- 🔄 JIT-компиляция
- 🔄 Стандартная библиотека
- 🔄 Пакетный менеджер
- 🔄 Отладчик

---

## 🤝 Участие в разработке

Мы приветствуем вклад в развитие проекта!

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/amazing`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing`)
5. Откройте Pull Request

---

## 📄 Лицензия

MIT License — используйте свободно для любых целей.

---

<p align="center">
  <strong>Pyrl</strong> — объединяем лучшее от Python и Perl
</p>
